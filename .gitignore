# --- Private agent repo (separate, closed repository) ---
agent/

# --- Claude / agent context files (live in the private agent repo) ---
CLAUDE.md
MEMORY.md
.claude/settings.local.json

# --- Secrets / environment ---
.env
.env.*
!.env.example

# --- Node ---
node_modules/
dist/
build/
.next/
coverage/
*.tsbuildinfo

# --- Data / runtime artifacts ---
data/
logs/
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db

# Local agent settings — one "git add -A" away from the public repo otherwise
.claude/
